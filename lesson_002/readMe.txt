

        《指针》
  定义:
	指向一个内存地址的变量，（这个不是正式的定义）。
  解释：
  定义一个变量 int a = 10; 在计算机内存中会给变量a 划分一个存储单元(64 位 占4个字节)，
这个存储单元有一个地址，并且这个地址是有编号的。那么指针则是一个变量，这个变量和一般
的变量有些不一样，正常的变量是代表地址上的一个具体值，而指针则是代表地址，两者的最大
差别。
  
  